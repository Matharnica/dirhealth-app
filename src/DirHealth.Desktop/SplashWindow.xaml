<Window x:Class="DirHealth.Desktop.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="320" Height="260"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" ShowInTaskbar="False">
    <Border Background="#0F1117" CornerRadius="12"
            BorderBrush="#2D3148" BorderThickness="1">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                    Margin="32,28,32,28">
            <Image Source="/Resources/icon_128.png"
                   Width="80" Height="80"
                   HorizontalAlignment="Center" Margin="0,0,0,16"
                   RenderOptions.BitmapScalingMode="HighQuality" />
            <TextBlock Text="DirHealth" FontSize="26" FontWeight="Bold"
                       Foreground="#6366F1" HorizontalAlignment="Center" />
            <TextBlock Text="Active Directory Health Monitor"
                       FontSize="11" Foreground="#94A3B8"
                       HorizontalAlignment="Center" Margin="0,4,0,24" />
            <ProgressBar IsIndeterminate="True" Width="180" Height="3"
                         Background="#1A1D27" Foreground="#6366F1"
                         BorderThickness="0" />
        </StackPanel>
    </Border>
</Window>
