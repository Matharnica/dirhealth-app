<Window x:Class="DirHealth.Desktop.Views.Login.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="DirHealth — Connect to Active Directory"
        Width="440" SizeToContent="Height"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="36" ResizeBorderThickness="0"
                            UseAeroCaptionButtons="True" GlassFrameThickness="0,0,0,0"/>
    </shell:WindowChrome.WindowChrome>
    <Grid Margin="36">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Connect to Active Directory"
                       FontSize="18" FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"/>
            <TextBlock Text="Domain Admin credentials are required to use DirHealth."
                       FontSize="12" Margin="0,4,0,0"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Domain -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Domain" FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Domain, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource DarkTextBox}"/>
        </StackPanel>

        <!-- Username -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="Username" FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource DarkTextBox}"/>
        </StackPanel>

        <!-- Password -->
        <StackPanel Grid.Row="6">
            <TextBlock Text="Password" FontSize="12"
                       Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
            <PasswordBox x:Name="PasswordBox"
                         Style="{StaticResource DarkPasswordBox}"/>
        </StackPanel>

        <!-- Remember credentials -->
        <CheckBox Grid.Row="8"
                  IsChecked="{Binding RememberCredentials}"
                  Foreground="{StaticResource TextSecondaryBrush}"
                  Content="Remember credentials"
                  FontSize="12"/>

        <!-- Error message -->
        <TextBlock Grid.Row="10"
                   Text="{Binding ErrorMessage}"
                   Foreground="#E05555"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}"/>

        <!-- Buttons -->
        <Grid Grid.Row="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButton}"/>

            <Button Grid.Column="2"
                    IsDefault="True"
                    IsEnabled="{Binding IsConnecting, Converter={StaticResource InverseBool}}"
                    Command="{Binding ConnectCommand}"
                    Style="{StaticResource PrimaryButton}"
                    MinWidth="100">
                <Button.Content>
                    <TextBlock>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Connect"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnecting}" Value="True">
                                        <Setter Property="Text" Value="Connecting..."/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Button.Content>
            </Button>
        </Grid>
    </Grid>
</Window>
